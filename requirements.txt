# Runtime: edge only. Domain and data layers are standard library (enforced
# by tests/test_layering.py). No LLM SDKs, no embeddings, by design (D1, D2).
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
httpx>=0.27
jinja2>=3.1
