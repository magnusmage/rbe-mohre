# Copy to .env and fill in. Never commit .env.
# Base URL of the RBE backend API, no trailing slash.
# Dev against a local control plane: http://localhost:8000
# Production (same-origin behind nginx): https://rbe.magnusmage.com
VITE_API_BASE_URL=http://localhost:8000
