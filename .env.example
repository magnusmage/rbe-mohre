# Copy to .env and fill in. Never commit .env.
# Generate tokens: python -c "import secrets;print(secrets.token_urlsafe(32))"
APP_ENV=dev
DATABASE_PATH=rbe.db
AGENT_TOOL_TOKEN=change-me-agent
REVIEWER_TOKEN=change-me-reviewer-different
ELEVENLABS_API_KEY=
ELEVENLABS_AGENT_ID=
ELEVENLABS_WEBHOOK_SECRET=
